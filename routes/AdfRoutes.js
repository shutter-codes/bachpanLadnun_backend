const express = require("express");
const { admissionFormDetailsPost } = require("../controller/AdfController");

const router = express.Router();


//router.route("/adf/new").post(admissionFormDetailsPost);

module.exports = router;
